<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pratishruti</title>
    <link rel="icon" href="/logo.png" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
      integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <link rel="stylesheet" href="/eventsCRUD.css" />
    <style>
      table tr td,
      th {
        padding: 0.3rem;
        text-align: center;
      }

      i {
        color: black;
      }

      button {
        border-radius: 0.5rem;
        background-color: black;
        color: white;
        border: none;
        padding: 0.2rem 0.4rem 0.2rem 0.4rem;
      }

      button:hover {
        background-color: gray;
      }

      input {
        border-radius: 0.6rem;
        border: 1px solid black;
        padding: 0.6rem;
        accent-color: black;
      }
    </style>
  </head>
  <body>
    <div class="main">
      <div class="head">
        <div class="return">
          <form action="/admin/view/<%= name %>" method="post">
            <button type="submit" title="Logout" id="return">
              <i class="fa-solid fa-arrow-left"></i>
            </button>
          </form>
        </div>
        <h2>Manage Section Cup Leader Board</h2>
      </div>
      <div class="board">
        <table border="black">
          <thead>
            <tr>
              <th>Section</th>
              <th>Score</th>
              <th>Update Score</th>
            </tr>
          </thead>
          <tbody>
            <% for (position of board) { %>
            <tr>
              <td><%= position.section %></td>
              <td><%= position.score %></td>
              <td>
                <input
                  type="number"
                  name="score"
                  style="width: 4rem"
                  placeholder="<%= position.score %>"
                  id="<%= position._id %>"
                  value="<%= position.score %>"
                />
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      <button type="button" class="updateBtn <%= name %>" style="margin-top: 0.4rem;">Update Scores</button>
    </div>
    <script src="/leader.js"></script>
  </body>
</html>
